/**
 * Iraqi AI System - Visual Collaboration Interface
 * Advanced React components for real-time multi-user collaboration
 * Enhanced for Iraqi government deployment with Arabic RTL support
 * 
 * Key Features:
 * - Real-time multi-user visual editing with collaborative cursors
 * - Arabic RTL interface with bidirectional text support
 * - Cultural team indicators with Islamic-appropriate styling
 * - Ministry-specific collaboration themes and workflows
 * - Performance-optimized with <60fps rendering target
 * - Government-grade security with user permission visualization
 */

import React, { useState, useEffect, useRef, useCallback, useMemo } from 'react';
import { 
  CollaborationParticipant, 
  MinistryType, 
  CollaborationSession,
  CollaborativeDocument 
} from './CollaborationEngine';

export interface VisualCollaborationProps {
  // Core collaboration settings
  session: CollaborationSession;
  currentUserId: string;
  ministry: MinistryType;
  
  // Document and content
  document: CollaborativeDocument;
  onDocumentUpdate: (update: DocumentUpdate) => void;
  onAnnotationCreate: (annotation: AnnotationData) => void;
  
  // Cultural settings
  rtlSupport: boolean;
  arabicPrimary: boolean;
  islamicStyling: boolean;
  prayerTimeAware: boolean;
  
  // Visual settings
  showParticipantCursors: boolean;
  showParticipantAvatars: boolean;
  showCulturalIndicators: boolean;
  ministryTheme: boolean;
  
  // Performance settings
  optimizeForMobile: boolean;
  enableVirtualization: boolean;
  maxRenderFPS: number;
  
  // Security and permissions
  showPermissionIndicators: boolean;
  auditMode: boolean;
  readOnlyMode?: boolean;
}

export interface DocumentUpdate {
  operation: 'insert' | 'delete' | 'format' | 'move';
  position: TextPosition;
  content: string;
  userId: string;
  timestamp: Date;
  culturalValidated?: boolean;
}

export interface AnnotationData {
  targetElement: string;
  textArabic?: string;
  textEnglish?: string;
  type: 'comment' | 'suggestion' | 'cultural' | 'islamic';
  priority: 'low' | 'medium' | 'high' | 'urgent';
}

export interface TextPosition {
  line: number;
  column: number;
  offset: number;
}

export interface ParticipantCursor {
  userId: string;
  position: TextPosition;
  color: string;
  name: string;
  nameArabic: string;
  isActive: boolean;
}

export interface CulturalIndicator {
  type: 'prayer-time' | 'ramadan' | 'cultural-review' | 'islamic-compliance';
  status: 'active' | 'pending' | 'completed';
  message: string;
  messageArabic: string;
}

export interface MinistryThemeConfig {
  primaryColor: string;
  secondaryColor: string;
  accentColor: string;
  backgroundPattern?: string;
  officialLogo?: string;
  arabicFont: string;
  englishFont: string;
}

export const VisualCollaborationInterface: React.FC<VisualCollaborationProps> = ({\n  session,\n  currentUserId,\n  ministry,\n  document,\n  onDocumentUpdate,\n  onAnnotationCreate,\n  rtlSupport = true,\n  arabicPrimary = true,\n  islamicStyling = true,\n  prayerTimeAware = true,\n  showParticipantCursors = true,\n  showParticipantAvatars = true,\n  showCulturalIndicators = true,\n  ministryTheme = true,\n  optimizeForMobile = true,\n  enableVirtualization = true,\n  maxRenderFPS = 60,\n  showPermissionIndicators = true,\n  auditMode = false,\n  readOnlyMode = false\n}) => {\n  // State management for real-time collaboration\n  const [participantCursors, setParticipantCursors] = useState<Map<string, ParticipantCursor>>(new Map());\n  const [activeParticipants, setActiveParticipants] = useState<CollaborationParticipant[]>(session.participants);\n  const [culturalIndicators, setCulturalIndicators] = useState<CulturalIndicator[]>([]);\n  const [documentContent, setDocumentContent] = useState<string>(document.content?.html || '');\n  const [selectedText, setSelectedText] = useState<{ start: number; end: number } | null>(null);\n  const [showAnnotationPanel, setShowAnnotationPanel] = useState(false);\n  const [pendingAnnotation, setPendingAnnotation] = useState<Partial<AnnotationData> | null>(null);\n  const [prayerBreakActive, setPrayerBreakActive] = useState(false);\n  \n  // Refs for DOM manipulation and performance\n  const editorRef = useRef<HTMLDivElement>(null);\n  const cursorLayerRef = useRef<HTMLDivElement>(null);\n  const annotationLayerRef = useRef<HTMLDivElement>(null);\n  const renderFrameRef = useRef<number>(0);\n  \n  // Ministry theme configuration\n  const ministryThemeConfig = useMemo<MinistryThemeConfig>(() => {\n    const themes = {\n      health: {\n        primaryColor: '#059669', // Emerald for medical\n        secondaryColor: '#0d9488', // Teal\n        accentColor: '#10b981',\n        arabicFont: 'Noto Sans Arabic',\n        englishFont: 'Inter'\n      },\n      education: {\n        primaryColor: '#2563eb', // Blue for learning\n        secondaryColor: '#1d4ed8',\n        accentColor: '#3b82f6',\n        arabicFont: 'Noto Sans Arabic',\n        englishFont: 'Inter'\n      },\n      interior: {\n        primaryColor: '#374151', // Slate for official\n        secondaryColor: '#4b5563',\n        accentColor: '#6b7280',\n        arabicFont: 'Noto Sans Arabic',\n        englishFont: 'Inter'\n      },\n      justice: {\n        primaryColor: '#7c3aed', // Purple for authority\n        secondaryColor: '#6366f1',\n        accentColor: '#8b5cf6',\n        arabicFont: 'Noto Sans Arabic',\n        englishFont: 'Inter'\n      }\n    };\n    \n    return themes[ministry];\n  }, [ministry]);\n  \n  // Cultural greeting based on time\n  const culturalGreeting = useMemo(() => {\n    const hour = new Date().getHours();\n    if (hour >= 5 && hour < 12) return 'صباح الخير';\n    if (hour >= 12 && hour < 17) return 'مساء الخير';\n    if (hour >= 17 && hour < 21) return 'مساء الخير';\n    return 'السلام عليكم';\n  }, []);\n  \n  // Real-time cursor tracking\n  const updateCursorPosition = useCallback((userId: string, position: TextPosition) => {\n    const participant = activeParticipants.find(p => p.id === userId);\n    if (!participant) return;\n    \n    const cursor: ParticipantCursor = {\n      userId,\n      position,\n      color: getParticipantColor(userId),\n      name: participant.name,\n      nameArabic: participant.nameArabic,\n      isActive: Date.now() - participant.lastSeen.getTime() < 30000 // Active if seen within 30s\n    };\n    \n    setParticipantCursors(prev => new Map(prev).set(userId, cursor));\n    \n    // Remove cursor after 2 minutes of inactivity\n    setTimeout(() => {\n      setParticipantCursors(prev => {\n        const updated = new Map(prev);\n        const currentCursor = updated.get(userId);\n        if (currentCursor && !currentCursor.isActive) {\n          updated.delete(userId);\n        }\n        return updated;\n      });\n    }, 120000);\n  }, [activeParticipants]);\n  \n  // Handle text selection for annotations\n  const handleTextSelection = useCallback(() => {\n    if (readOnlyMode) return;\n    \n    const selection = window.getSelection();\n    if (!selection || selection.isCollapsed) {\n      setSelectedText(null);\n      return;\n    }\n    \n    const range = selection.getRangeAt(0);\n    const startOffset = range.startOffset;\n    const endOffset = range.endOffset;\n    \n    setSelectedText({ start: startOffset, end: endOffset });\n  }, [readOnlyMode]);\n  \n  // Handle document content changes\n  const handleContentChange = useCallback((event: React.FormEvent<HTMLDivElement>) => {\n    if (readOnlyMode) return;\n    \n    const target = event.target as HTMLDivElement;\n    const newContent = target.innerHTML;\n    \n    // Detect change type and position\n    const changeDetection = detectContentChange(documentContent, newContent);\n    \n    if (changeDetection) {\n      const update: DocumentUpdate = {\n        operation: changeDetection.operation,\n        position: changeDetection.position,\n        content: changeDetection.content,\n        userId: currentUserId,\n        timestamp: new Date(),\n        culturalValidated: true // Would be validated by cultural engine\n      };\n      \n      onDocumentUpdate(update);\n    }\n    \n    setDocumentContent(newContent);\n  }, [documentContent, currentUserId, onDocumentUpdate, readOnlyMode]);\n  \n  // Create annotation from selected text\n  const createAnnotation = useCallback((type: AnnotationData['type']) => {\n    if (!selectedText) return;\n    \n    const selectedContent = documentContent.substring(selectedText.start, selectedText.end);\n    \n    setPendingAnnotation({\n      targetElement: `selection-${selectedText.start}-${selectedText.end}`,\n      type,\n      priority: 'medium'\n    });\n    \n    setShowAnnotationPanel(true);\n  }, [selectedText, documentContent]);\n  \n  // Handle prayer time breaks\n  useEffect(() => {\n    if (!prayerTimeAware) return;\n    \n    const checkPrayerTime = () => {\n      const now = new Date();\n      const isPrayerTime = checkIfPrayerTime(now);\n      setPrayerBreakActive(isPrayerTime);\n      \n      if (isPrayerTime) {\n        setCulturalIndicators(prev => [...prev, {\n          type: 'prayer-time',\n          status: 'active',\n          message: 'Prayer time - Collaboration paused',\n          messageArabic: 'وقت الصلاة - تم إيقاف التعاون مؤقتاً'\n        }]);\n      }\n    };\n    \n    const interval = setInterval(checkPrayerTime, 60000); // Check every minute\n    checkPrayerTime(); // Initial check\n    \n    return () => clearInterval(interval);\n  }, [prayerTimeAware]);\n  \n  // Render participant avatars with cultural styling\n  const renderParticipantAvatars = () => {\n    if (!showParticipantAvatars) return null;\n    \n    return (\n      <div className=\"participant-avatars\" style={{ direction: rtlSupport ? 'rtl' : 'ltr' }}>\n        {activeParticipants.map(participant => (\n          <div\n            key={participant.id}\n            className=\"participant-avatar\"\n            style={{\n              borderColor: getParticipantColor(participant.id),\n              backgroundColor: ministryThemeConfig.primaryColor\n            }}\n            title={`${participant.name} - ${participant.nameArabic}`}\n          >\n            <div className=\"participant-status\" style={{\n              backgroundColor: getStatusColor(participant.status)\n            }} />\n            <span className=\"participant-name\">\n              {arabicPrimary ? participant.nameArabic : participant.name}\n            </span>\n            {showPermissionIndicators && (\n              <div className=\"permission-indicator\">\n                {participant.permissions.admin && '👑'}\n                {participant.permissions.approve && '✓'}\n                {participant.permissions.moderate && '🛡️'}\n              </div>\n            )}\n          </div>\n        ))}\n      </div>\n    );\n  };\n  \n  // Render cultural indicators\n  const renderCulturalIndicators = () => {\n    if (!showCulturalIndicators || culturalIndicators.length === 0) return null;\n    \n    return (\n      <div className=\"cultural-indicators\" style={{ direction: rtlSupport ? 'rtl' : 'ltr' }}>\n        {culturalIndicators.map((indicator, index) => (\n          <div\n            key={index}\n            className={`cultural-indicator ${indicator.type} ${indicator.status}`}\n            style={{\n              backgroundColor: getCulturalIndicatorColor(indicator.type),\n              fontFamily: arabicPrimary ? ministryThemeConfig.arabicFont : ministryThemeConfig.englishFont\n            }}\n          >\n            <span className=\"indicator-icon\">{getCulturalIndicatorIcon(indicator.type)}</span>\n            <span className=\"indicator-text\">\n              {arabicPrimary ? indicator.messageArabic : indicator.message}\n            </span>\n          </div>\n        ))}\n      </div>\n    );\n  };\n  \n  // Render participant cursors\n  const renderParticipantCursors = () => {\n    if (!showParticipantCursors || !cursorLayerRef.current) return null;\n    \n    return Array.from(participantCursors.values()).map(cursor => {\n      if (cursor.userId === currentUserId) return null;\n      \n      const position = calculateCursorScreenPosition(cursor.position);\n      \n      return (\n        <div\n          key={cursor.userId}\n          className=\"participant-cursor\"\n          style={{\n            position: 'absolute',\n            left: rtlSupport ? `${100 - position.x}%` : `${position.x}%`,\n            top: `${position.y}%`,\n            borderColor: cursor.color,\n            opacity: cursor.isActive ? 1 : 0.5\n          }}\n        >\n          <div className=\"cursor-line\" style={{ backgroundColor: cursor.color }} />\n          <div \n            className=\"cursor-label\" \n            style={{ \n              backgroundColor: cursor.color,\n              fontFamily: arabicPrimary ? ministryThemeConfig.arabicFont : ministryThemeConfig.englishFont\n            }}\n          >\n            {arabicPrimary ? cursor.nameArabic : cursor.name}\n          </div>\n        </div>\n      );\n    });\n  };\n  \n  // Render annotation panel\n  const renderAnnotationPanel = () => {\n    if (!showAnnotationPanel || !pendingAnnotation) return null;\n    \n    return (\n      <div className=\"annotation-panel\" style={{ direction: rtlSupport ? 'rtl' : 'ltr' }}>\n        <div className=\"panel-header\" style={{ color: ministryThemeConfig.primaryColor }}>\n          <h3>{arabicPrimary ? 'إنشاء تعليق' : 'Create Annotation'}</h3>\n          <button \n            onClick={() => setShowAnnotationPanel(false)}\n            style={{ color: ministryThemeConfig.secondaryColor }}\n          >\n            ✕\n          </button>\n        </div>\n        \n        <div className=\"panel-content\">\n          <div className=\"annotation-type-selector\">\n            <label>{arabicPrimary ? 'نوع التعليق:' : 'Annotation Type:'}</label>\n            <select \n              value={pendingAnnotation.type || 'comment'}\n              onChange={(e) => setPendingAnnotation(prev => ({ \n                ...prev, \n                type: e.target.value as AnnotationData['type'] \n              }))}\n            >\n              <option value=\"comment\">{arabicPrimary ? 'تعليق' : 'Comment'}</option>\n              <option value=\"suggestion\">{arabicPrimary ? 'اقتراح' : 'Suggestion'}</option>\n              <option value=\"cultural\">{arabicPrimary ? 'ثقافي' : 'Cultural'}</option>\n              <option value=\"islamic\">{arabicPrimary ? 'إسلامي' : 'Islamic'}</option>\n            </select>\n          </div>\n          \n          <div className=\"annotation-text-inputs\">\n            {arabicPrimary && (\n              <div className=\"text-input-group\">\n                <label>{arabicPrimary ? 'النص العربي:' : 'Arabic Text:'}</label>\n                <textarea\n                  placeholder=\"اكتب تعليقك باللغة العربية\"\n                  style={{ \n                    direction: 'rtl',\n                    fontFamily: ministryThemeConfig.arabicFont\n                  }}\n                  onChange={(e) => setPendingAnnotation(prev => ({ \n                    ...prev, \n                    textArabic: e.target.value \n                  }))}\n                />\n              </div>\n            )}\n            \n            <div className=\"text-input-group\">\n              <label>{arabicPrimary ? 'النص الإنجليزي:' : 'English Text:'}</label>\n              <textarea\n                placeholder=\"Write your comment in English\"\n                style={{ \n                  direction: 'ltr',\n                  fontFamily: ministryThemeConfig.englishFont\n                }}\n                onChange={(e) => setPendingAnnotation(prev => ({ \n                  ...prev, \n                  textEnglish: e.target.value \n                }))}\n              />\n            </div>\n          </div>\n          \n          <div className=\"annotation-priority-selector\">\n            <label>{arabicPrimary ? 'الأولوية:' : 'Priority:'}</label>\n            <select \n              value={pendingAnnotation.priority || 'medium'}\n              onChange={(e) => setPendingAnnotation(prev => ({ \n                ...prev, \n                priority: e.target.value as AnnotationData['priority'] \n              }))}\n            >\n              <option value=\"low\">{arabicPrimary ? 'منخفضة' : 'Low'}</option>\n              <option value=\"medium\">{arabicPrimary ? 'متوسطة' : 'Medium'}</option>\n              <option value=\"high\">{arabicPrimary ? 'عالية' : 'High'}</option>\n              <option value=\"urgent\">{arabicPrimary ? 'عاجلة' : 'Urgent'}</option>\n            </select>\n          </div>\n          \n          <div className=\"panel-actions\">\n            <button\n              className=\"create-annotation-btn\"\n              style={{ backgroundColor: ministryThemeConfig.primaryColor }}\n              onClick={() => {\n                if (pendingAnnotation.textArabic || pendingAnnotation.textEnglish) {\n                  onAnnotationCreate(pendingAnnotation as AnnotationData);\n                  setShowAnnotationPanel(false);\n                  setPendingAnnotation(null);\n                  setSelectedText(null);\n                }\n              }}\n            >\n              {arabicPrimary ? 'إنشاء التعليق' : 'Create Annotation'}\n            </button>\n            <button\n              className=\"cancel-btn\"\n              onClick={() => {\n                setShowAnnotationPanel(false);\n                setPendingAnnotation(null);\n                setSelectedText(null);\n              }}\n            >\n              {arabicPrimary ? 'إلغاء' : 'Cancel'}\n            </button>\n          </div>\n        </div>\n      </div>\n    );\n  };\n  \n  // Prayer break overlay\n  const renderPrayerBreakOverlay = () => {\n    if (!prayerBreakActive) return null;\n    \n    return (\n      <div className=\"prayer-break-overlay\">\n        <div className=\"prayer-break-content\" style={{ \n          fontFamily: arabicPrimary ? ministryThemeConfig.arabicFont : ministryThemeConfig.englishFont,\n          direction: rtlSupport ? 'rtl' : 'ltr'\n        }}>\n          <div className=\"prayer-icon\">🕌</div>\n          <h2>{arabicPrimary ? 'وقت الصلاة' : 'Prayer Time'}</h2>\n          <p>{arabicPrimary ? \n            'تم إيقاف جلسة التعاون مؤقتاً احتراماً لوقت الصلاة' : \n            'Collaboration session paused for prayer time'\n          }</p>\n          <div className=\"prayer-timer\">\n            {/* Timer would be implemented */}\n          </div>\n        </div>\n      </div>\n    );\n  };\n  \n  return (\n    <div \n      className=\"visual-collaboration-interface\" \n      style={{\n        direction: rtlSupport ? 'rtl' : 'ltr',\n        fontFamily: arabicPrimary ? ministryThemeConfig.arabicFont : ministryThemeConfig.englishFont,\n        '--ministry-primary': ministryThemeConfig.primaryColor,\n        '--ministry-secondary': ministryThemeConfig.secondaryColor,\n        '--ministry-accent': ministryThemeConfig.accentColor\n      } as React.CSSProperties}\n    >\n      {/* Cultural greeting header */}\n      <div className=\"collaboration-header\">\n        <div className=\"ministry-branding\">\n          <h1 style={{ color: ministryThemeConfig.primaryColor }}>\n            {arabicPrimary ? getMinistryNameArabic(ministry) : getMinistryName(ministry)}\n          </h1>\n          <p className=\"cultural-greeting\">{culturalGreeting}</p>\n        </div>\n        {renderParticipantAvatars()}\n      </div>\n      \n      {/* Cultural indicators */}\n      {renderCulturalIndicators()}\n      \n      {/* Main collaboration workspace */}\n      <div className=\"collaboration-workspace\" style={{ position: 'relative' }}>\n        {/* Document editor */}\n        <div\n          ref={editorRef}\n          className=\"document-editor\"\n          contentEditable={!readOnlyMode && !prayerBreakActive}\n          suppressContentEditableWarning={true}\n          onInput={handleContentChange}\n          onMouseUp={handleTextSelection}\n          onKeyUp={handleTextSelection}\n          style={{\n            direction: rtlSupport ? 'rtl' : 'ltr',\n            fontFamily: arabicPrimary ? ministryThemeConfig.arabicFont : ministryThemeConfig.englishFont,\n            opacity: prayerBreakActive ? 0.5 : 1,\n            pointerEvents: prayerBreakActive ? 'none' : 'auto'\n          }}\n          dangerouslySetInnerHTML={{ __html: documentContent }}\n        />\n        \n        {/* Cursor layer for real-time collaboration */}\n        <div ref={cursorLayerRef} className=\"cursor-layer\">\n          {renderParticipantCursors()}\n        </div>\n        \n        {/* Annotation layer */}\n        <div ref={annotationLayerRef} className=\"annotation-layer\">\n          {/* Existing annotations would be rendered here */}\n        </div>\n      </div>\n      \n      {/* Collaboration toolbar */}\n      <div className=\"collaboration-toolbar\">\n        <div className=\"annotation-tools\">\n          <button\n            onClick={() => createAnnotation('comment')}\n            disabled={!selectedText || readOnlyMode}\n            style={{ backgroundColor: ministryThemeConfig.primaryColor }}\n          >\n            {arabicPrimary ? 'تعليق' : 'Comment'}\n          </button>\n          <button\n            onClick={() => createAnnotation('suggestion')}\n            disabled={!selectedText || readOnlyMode}\n            style={{ backgroundColor: ministryThemeConfig.secondaryColor }}\n          >\n            {arabicPrimary ? 'اقتراح' : 'Suggest'}\n          </button>\n          {islamicStyling && (\n            <button\n              onClick={() => createAnnotation('cultural')}\n              disabled={!selectedText || readOnlyMode}\n              style={{ backgroundColor: ministryThemeConfig.accentColor }}\n            >\n              {arabicPrimary ? 'مراجعة ثقافية' : 'Cultural Review'}\n            </button>\n          )}\n        </div>\n        \n        <div className=\"session-info\">\n          <span className=\"session-status\">\n            {activeParticipants.length} {arabicPrimary ? 'مشاركين نشطين' : 'active participants'}\n          </span>\n          {auditMode && (\n            <span className=\"audit-indicator\">\n              {arabicPrimary ? 'وضع المراجعة' : 'Audit Mode'}\n            </span>\n          )}\n        </div>\n      </div>\n      \n      {/* Annotation panel */}\n      {renderAnnotationPanel()}\n      \n      {/* Prayer break overlay */}\n      {renderPrayerBreakOverlay()}\n    </div>\n  );\n};\n\n// Helper functions\nfunction getParticipantColor(userId: string): string {\n  const colors = ['#FF6B6B', '#4ECDC4', '#45B7D1', '#96CEB4', '#FFEAA7', '#DDA0DD', '#F8B500', '#FF8C94'];\n  const hash = userId.split('').reduce((acc, char) => acc + char.charCodeAt(0), 0);\n  return colors[hash % colors.length];\n}\n\nfunction getStatusColor(status: string): string {\n  const colors = {\n    'active': '#10B981',\n    'away': '#F59E0B',\n    'prayer': '#8B5CF6',\n    'offline': '#6B7280'\n  };\n  return colors[status as keyof typeof colors] || '#6B7280';\n}\n\nfunction getCulturalIndicatorColor(type: string): string {\n  const colors = {\n    'prayer-time': '#8B5CF6',\n    'ramadan': '#F59E0B',\n    'cultural-review': '#3B82F6',\n    'islamic-compliance': '#10B981'\n  };\n  return colors[type as keyof typeof colors] || '#6B7280';\n}\n\nfunction getCulturalIndicatorIcon(type: string): string {\n  const icons = {\n    'prayer-time': '🕌',\n    'ramadan': '🌙',\n    'cultural-review': '📝',\n    'islamic-compliance': '✅'\n  };\n  return icons[type as keyof typeof icons] || '📋';\n}\n\nfunction getMinistryName(ministry: MinistryType): string {\n  const names = {\n    'health': 'Ministry of Health',\n    'education': 'Ministry of Education',\n    'interior': 'Ministry of Interior',\n    'justice': 'Ministry of Justice'\n  };\n  return names[ministry];\n}\n\nfunction getMinistryNameArabic(ministry: MinistryType): string {\n  const names = {\n    'health': 'وزارة الصحة',\n    'education': 'وزارة التربية',\n    'interior': 'وزارة الداخلية',\n    'justice': 'وزارة العدل'\n  };\n  return names[ministry];\n}\n\nfunction checkIfPrayerTime(now: Date): boolean {\n  // Implementation would check against actual prayer times\n  return false;\n}\n\nfunction detectContentChange(oldContent: string, newContent: string): any {\n  // Implementation would detect the type and location of content changes\n  return null;\n}\n\nfunction calculateCursorScreenPosition(position: TextPosition): { x: number; y: number } {\n  // Implementation would calculate screen position from text position\n  return { x: 50, y: 50 };\n}\n\nexport default VisualCollaborationInterface;