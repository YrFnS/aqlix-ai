# Execute Iraqi AI Pydantic Agent PRP with Archon Management

Implement a Pydantic AI agent using the PRP file with systematic Archon task management and Iraqi AI cultural integration.

## PRP File: $ARGUMENTS

## MANDATORY: Archon Task Management Workflow

**CRITICAL RULE: NEVER skip Archon task management at any point in this process.**

### Pre-Implementation Setup
1. **Create Project in Archon** (if not exists)
   - Use `mcp__archon__manage_project` to ensure project exists
   - Project name: "Iraqi AI Chat System - Agents"

2. **Create ALL Agent PRP Tasks in Archon**
   - Parse PRP requirements into discrete agent development tasks
   - Create each task with `mcp__archon__manage_task("create", ...)`
   - Set all initial status to "todo"
   - Include clear acceptance criteria for each task

## Execution Process

### 1. **Load PRP with Archon Tracking**
   - Read the specified Pydantic AI PRP file
   - **Create Archon task**: "Analyze Agent PRP requirements"
   - Update task status to "doing" with `mcp__archon__manage_task("update", task_id=..., status="doing")`
   - Understand all agent requirements and research findings
   - Review main_agent_reference patterns for implementation guidance
   - **Create Archon task**: "Research Pydantic AI patterns"
   - Do web searches and Pydantic AI documentation review as needed
   - **Complete research tasks** and mark as "done"

### 2. **ULTRATHINK with Iraqi AI Agent Architecture**
   - **Create Archon task**: "Plan Iraqi AI agent implementation"
   - Update to "doing" status
   - Use `iraqi-ai-agent-architect` for culturally-aware agent design
   - Break down agent development into specific Archon tasks:
     a. Agent configuration and setup
     b. Cultural context integration
     c. Arabic language processing tools
     d. Iraqi domain-specific tools
     e. Testing and validation
   - Follow main_agent_reference patterns with Iraqi enhancements
   - Plan agent.py, tools.py, models.py, settings.py structure
   - **Complete planning task** and mark as "done"

### 3. **Execute with Sequential Iraqi AI Integration**
   - **RULE**: Only ONE task in "doing" status at a time
   - For each agent development task:
     a. Update task status to "doing"
     b. Use specialized Iraqi AI agents:
        - `iraqi-ai-agent-architect` for agent design
        - `iraqi-cultural-validator` for cultural compliance
        - `arabic-rtl-processor` for Arabic processing integration
        - `iraqi-technical-debugger` for debugging agent issues
        - `iraqi-security-specialist` for agent security
     c. Implement the specific agent component:
        - **Agent Core**: Environment-based configuration with Iraqi context
        - **Cultural Tools**: @agent.tool decorators with cultural awareness
        - **Arabic Integration**: RTL and dialect processing capabilities
        - **Security**: Environment variables, proper error handling
     d. **Complete task** and mark as "done"
     e. Move to next task

### 4. **Validate with Iraqi AI Testing**
   - **Create Archon task**: "Agent validation and testing"
   - Update to "doing" status
   - Use `iraqi-cultural-tester` for cultural validation testing
   - Test agent import and instantiation
   - Run TestModel validation for rapid development testing
   - Test Arabic processing tools functionality
   - Test cultural compliance features
   - Run pytest test suite with Iraqi scenarios
   - Verify agent follows main_agent_reference + Iraqi patterns
   - **Complete validation task** and mark as "done"

### 5. **Complete with Iraqi AI Review**
   - **Create Archon task**: "Final agent completion review"
   - Update to "doing" status
   - Ensure all PRP checklist items completed
   - Test agent with Iraqi-specific example queries
   - Verify cultural compliance and Arabic processing
   - Test security patterns (environment variables, error handling)
   - Cross-reference with original PRP requirements
   - **Complete final task** and mark as "done"

### 6. **Archon Error Handling**
   - If Archon operations fail, retry once
   - Document any persistent Archon issues
   - Use TodoWrite as backup task tracking if needed
   - Continue with systematic workflow regardless

## Iraqi AI Pydantic Agent Patterns

### Cultural Integration Requirements
- **Context Awareness**: Integrate Iraqi cultural context in agent system prompt
- **Language Support**: Build-in Arabic/English code-switching capabilities
- **Professional Domains**: Support Iraqi legal, medical, educational contexts
- **Islamic Compliance**: Ensure all agent responses respect Islamic values

### Technical Patterns to Follow
- **Configuration**: Use environment-based setup + Iraqi cultural config
- **Output**: Default to string output with Arabic RTL awareness
- **Tools**: Use @agent.tool with RunContext + cultural validation
- **Testing**: Include TestModel + Iraqi cultural test scenarios
- **Security**: Environment variables + cultural content filtering

### Iraqi AI Agent Integration
- **Cultural Validation**: Always use `iraqi-cultural-validator` for agent responses
- **Arabic Processing**: Always use `arabic-rtl-processor` for text handling
- **Security**: Always use `iraqi-security-specialist` for agent security
- **Architecture**: Always use `iraqi-ai-agent-architect` for design decisions

**Remember**: Archon provides centralized coordination while Iraqi AI agents provide specialized cultural and technical expertise for Pydantic AI agent development.