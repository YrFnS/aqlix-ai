/**
 * Iraqi AI Chat System - API Client Package
 *
 * This package will contain shared API client logic for communication
 * between frontend applications and the FastAPI backend.
 *
 * Features (to be implemented in future PRPs):
 * - HTTP client with automatic retries
 * - Request/response interceptors
 * - Type-safe API endpoints
 * - Authentication token management
 * - Error handling and logging
 *
 * @packageDocumentation
 */

// Placeholder export to make package buildable
export const API_CLIENT_VERSION = '1.0.0';

/**
 * Placeholder type - will be replaced with actual API client implementation
 */
export type ApiClient = {
  version: string;
};
